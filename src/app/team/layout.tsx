import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Enthuzea 2k26 — Team",
  description: "Meet the passionate team behind Enthuzea 2k26 — the organizers, performers, and creative minds driving SVIST's grandest cultural fest.",
};

export default function TeamLayout({ children }: { children: React.ReactNode }) {
  return children;
}
