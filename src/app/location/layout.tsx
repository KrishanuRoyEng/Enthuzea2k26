import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Enthuzea 2k26 — Location",
  description: "Find us at SVIST — get directions, venue details, and everything you need to reach Enthuzea 2k26 on 21st & 22nd July 2026.",
};

export default function LocationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
